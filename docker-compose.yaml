services:
  llm-api-test:
    build:
      context: .
      dockerfile: Dockerfile
    image: llm-api-test:latest
    container_name: llm-api-test
    # ports:
    #   - "8080:80"
    restart: unless-stopped
